"""评估 Prompt：LLM-as-judge 对客服表现打分（第9期）。

三个 judge 对应需要「理解语义」才能判定、规则代码搞不定的维度：
- ANSWER_QUALITY_PROMPT：回答质量（结果指标）
- HALLUCINATION_PROMPT：是否对照工具返回产生幻觉（结果指标）
- PROCESS_SOUNDNESS_PROMPT：工具选择与顺序是否合理（过程指标）

均要求 temperature=0.0、只输出 JSON（JSON 示例用 {{ }} 转义以兼容 .format）。
"""

ANSWER_QUALITY_PROMPT = """你是电商客服质量评审专家。请根据用户问题和客服回复，对回复的「响应质量」打分。

【用户问题】
{user_input}

【客服回复】
{reply}

【参考要点】（可选，用于判断回复是否覆盖了应有信息，"（无）"表示不提供参考）
{reference}

评分维度：是否准确、完整、专业、语气得体，是否直接解决了用户问题。
- 5 分：准确、完整、专业、语气得体，直接解决问题，无多余或错误信息
- 4 分：基本解决问题，信息准确，但略有冗余或可改进之处
- 3 分：部分解决问题，或信息不够完整/不够清晰
- 2 分：回复偏离问题，或包含明显不准确信息
- 1 分：完全没有解决问题，答非所问，或包含严重错误

只输出 JSON，不要加 markdown 代码块：
{{
  "score": <1到5的整数>,
  "reason": "<一句话中文评分理由>"
}}"""


TASK_COMPLETION_PROMPT = """你是电商客服任务完成度评审专家。请根据用户输入、客服回复以及期望完成的任务列表，评估 Agent 是否完成了所有预期任务。

【用户输入】
{user_input}

【客服回复】
{reply}

【期望完成的任务列表】
{expected_tasks}

评分维度：Agent 回复是否完成了所有期望任务，每个任务是否有明确的结果确认。
- 5 分：所有任务全部完成，且每个任务都有明确的结果确认（如：告知了物流信息、提供了订单状态、用户明确收到答复）
- 4 分：所有任务完成，但部分任务的结果未向用户明确确认（如调用了工具但回复中未提及关键信息）
- 3 分：主要任务完成，但遗漏了次要任务（如查询了订单但未告知物流）
- 2 分：只完成了部分任务，关键任务未完成（如只查了订单但没回复用户）
- 1 分：几乎没有完成任务，答非所问，或完全未执行预期操作

只输出 JSON，不要加 markdown 代码块：
{{
  "score": <1到5的整数>,
  "reason": "<一句话中文评分理由>"
}}"""


HALLUCINATION_PROMPT = """你是电商客服事实核查专家。请核查客服回复是否「忠实」于工具返回的数据。

【客服回复】
{reply}

【本次会话的工具返回数据】（这是唯一可信的事实来源）
{observations}

════════════════════════════════════════
核心原则：只核查具体事实错误
════════════════════════════════════════
你的唯一任务是判断：回复中的「具体事实」是否与工具返回的数据矛盾。
- ✅ 要检查：订单号、金额、状态、时间、数量、名称、物流节点等具体事实是否准确
- ❌ 不检查：表达风格、语气、排版格式、冗余描述、修辞手法

════════════════════════════════════════
硬性豁免规则 —— 必须判为 faithful=true
════════════════════════════════════════
以下情况一律视为忠实，不得判为幻觉：

1. 【格式美化】语气词、emoji、markdown 表格、千分位分隔符、日期/时间格式转换
   （如 "亲"、"😊"、"5,067.21"、"2024年3月"→"2024-03-15"）

2. 【语言转换】英文公司名/快递名翻译为中文，或反之
   （如 "ZTO Express"↔"中通快递"、"UPS"↔"联合包裹"）

3. 【合理推断】对工具返回的正确解读和合理推断
   （如工具返回不含 Levi's 501，Agent 说"没找到 Levi's 501"）

4. 【礼貌用语】问候语、感谢语、致歉语、祝福语等社交礼仪
   （如"您好"、"感谢您的耐心"、"抱歉给您带来不便"、"祝您生活愉快"）

5. 【公司名截断】为简洁而截断公司名称/商品全称，但未改变实质信息
   （如"京东"代替"京东科技"、"天猫超市"代替完整店铺名）

6. 【结构化展示】将工具返回的 JSON/列表转换为表格、列表等更易读的格式
   只要数据本身未篡改，格式变化不构成幻觉

7. 【政策/常识补充】补充与事实无关的一般性政策说明或行业常识
   （如"支持7天无理由退换"、"正品保证"），前提是不与工具数据矛盾

════════════════════════════════════════
判断标准
════════════════════════════════════════

faithful = true（忠实）：
- 回复中的具体事实（订单状态、价格、物流节点、库存等）都能在工具返回中找到依据，或
- 回复未涉及需要核查的具体事实（如纯问候、一般性政策说明），或
- 仅触发了上述硬性豁免规则中的任何一条，或
- 【无罪推定】即使回复包含排版美化、格式转换或语气增强，只要核心事实正确，必须判为 faithful=true

faithful = false（幻觉）：
- 回复中出现了工具返回里「没有」且「与之矛盾」的具体事实
- 典型幻觉：编造价格、虚构物流进度、谎称有货、捏造订单状态、篡改商品数量
- 只有当具体事实确实与工具数据冲突时，才判为 faithful=false

════════════════════════════════════════
只输出 JSON，不要加 markdown 代码块：
{{
  "faithful": <true或false>,
  "reason": "<一句话中文核查理由，指出依据或矛盾点>"
}}"""


PROCESS_SOUNDNESS_PROMPT = """你是 Agent 推理过程评审专家。请判断客服 Agent 为解决用户问题而调用工具的「过程」是否合理。

【用户问题】
{user_input}

【实际工具调用序列】（按先后顺序，"（未调用任何工具）"表示没有调用）
{tool_sequence}

评分维度：工具选择是否对路、顺序是否合理、有无明显多余或缺失的调用。
- 5 分：工具选择精准、顺序合理、无多余调用
- 4 分：基本合理，略有可优化空间
- 3 分：能解决问题但有冗余调用或顺序欠佳
- 2 分：调用了不相关工具，或缺少关键调用
- 1 分：过程明显错误（答非所问该调不调、或大量无关调用）

只输出 JSON，不要加 markdown 代码块：
{{
  "score": <1到5的整数>,
  "reason": "<一句话中文评分理由>"
}}"""

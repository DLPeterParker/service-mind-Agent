"""统一记忆提取 Prompt：一次 LLM 调用，三层分流（STM / Profile / LTM）。"""

UNIFIED_EXTRACTION_PROMPT = """你是对话分析助手。基于下面的电商客服对话片段，提取三类信息：

## 输出格式
严格输出 JSON 格式，包含三个字段：
{{
  "session_facts": ["事实1", "事实2"],
  "profile_updates": {{"key": "value"}},
  "new_ltm_facts": ["事实1", "事实2"]
}}

## 字段说明

### 1. session_facts（短期记忆）
基于最近 N 条消息，提取当前会话的活跃事实。例如：
- "用户想退订单 ORD-001"
- "原因是尺码不合适"
- "用户希望今天内完成退款"

要求：
- 每条是完整的中文句子
- 只提取当前会话中活跃的事实
- 不要求判断"是否已解决"
- 如果当前会话没有新信息，返回空数组 []

### 2. profile_updates（用户画像更新）
用户明确提到的稳定个人信息，key 用英文下划线命名。例如：
- {"shoe_size": "42"}
- {"preferred_color": "红色"}
- {"member_level": "钻石会员"}

要求：
- key 必须是英文下划线命名
- 只记录稳定、跨会话有效的信息
- 如果本轮没有新信息，返回空对象 {{}}

### 3. new_ltm_facts（长期记忆事实）
值得跨会话记住的事实，用一句话概括。例如：
- "用户偏好 42 码鞋子"
- "用户曾于 2024-01 申请过退款"

要求：
- 简洁的中文句子
- 只记录值得长期记住的事实
- 如果本轮没有新信息，返回空数组 []

## 对话内容
{transcript}
"""

__all__ = ["UNIFIED_EXTRACTION_PROMPT"]
